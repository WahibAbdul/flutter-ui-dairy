class AppConstants {
  static const apiDateFormat = "dd-MM-yyyy";
  static const apiTimeFormat = "hh:mm a";
  static const apiDateTimeFormat = "yyyy-MM-dd HH:mm:ss";

  static const appDateFormat = "dd/MM/yyyy";
  static const appTimeFormat = "hh:mm a";
  static const appDateTimeFormat = "hh:mm a dd/MMM/yy";

  static const defaultScheduleDurationInHours = 4;
  static const maxNurseOneDaySchedules = 3;
  static const maxPatientOneDaySchedules = 1;
}

class Routes {}

class WidgetConstants {
  static const cornerRadius = 20.0;
  static const textFieldRadius = 20.0;
  static const cardHeight = 140.0;
}

class FirebaseTopics {
  static const general = "General";
}
